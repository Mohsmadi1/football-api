using football_api.Models;

namespace football_api.Helpers;

public static class ExtensionMethods
{
    public static void Destroy(this XObject xObject, string name)
    {
        var x = 5 + 5;
        Console.WriteLine(name);
    }

}